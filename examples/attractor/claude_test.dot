// Minimal test: single LLM node via Claude Code CLI
digraph claude_test {
    graph [goal="Test Claude Code backend integration"]

    start [shape=Mdiamond]
    ask [shape=box, label="Ask Claude"
        prompt="Reply with exactly: ATTRACTOR_OK. Nothing else."]
    finish [shape=Msquare]

    start -> ask -> finish
}
